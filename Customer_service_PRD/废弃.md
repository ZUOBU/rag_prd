## 16.2 长期记忆
- **生命周期**：永久保存。静态字段由行内系统定时跑批同步；非通用隐式偏好由大模型定时剥离异步萃取。
- **存储方式**：
  - **静态全量结构化事实现状**：MySQL / PostgreSQL（存储标签、职级权限、资产标尺）。
  - **语义表征动态提取池**：向量数据库 Vector DB（存储从文本流水中异步萃取的非结构化碎片）。

### 16.2.1 面向内部员工 (Staff)
- **字段**：
  - `emp_id`: 员工工号。
  - `dept_code`: 隶属业务条线代码。**（可用作不同知识库查询的优先级）。**
  - `job_title`: 职级/岗位（可用作鉴权）。
  - `branch_region`: 归属网点代码（拉取属地特异性版本或政策）。
  - `frequent_kb_areas`: 历史高频查询聚类（用于前端推荐专栏加载预热，待加入）。

### 16.2.2 面向外部客户 (Customer)
- **核心字段 (综合库同步)**：
  - `customer_id`: 客户唯一标识。
  - `basic_profile`: 静态基础统计信息（包含：`age`年龄、`gender`性别、`residence_city`常住地）。
  - `cust_tier_level`: 客户等级（大众散客、金葵花、私行等）。**（核心作用：强力防幻觉，阻断私行条款外泄给普通长尾客引发系统级客诉风控事故）。**
  - `risk_tolerance_level`: 风险承受力等级（R1保守至R5激进）。生成包含关联高息产品回答时，执行不可退撤的合规兜底。
  - `holdings_tag_flags`: 持仓标记布尔值数组集合（如 `has_mortgage`, `has_credit_card`，不暴露确切资金余额，仅辅导大模型掌握推荐语境）。
  - `extracted_preferences` (依托Vector DB存储): 大模型从历史会话长文中提取降维的非格式化隐式偏好向量实体。例如：“客户抵触超长锁仓型结构产品”或“备注周四不开机拒接电销”。

#### 14.3.1 坐席登录与权限初始化
1. **登录认证**：坐席输入工号和密码。
2. **权限加载**：系统根据 `emp_id` 加载 `dept_code`, `job_title`, `branch_region` 等信息。
3. **知识库预热**：根据 `dept_code` 加载对应的知识库分类，并预加载高频查询。

4. **知识库更新**：
   - **实时更新**：坐席的编辑内容实时更新到 `dialogue_history`。
   - **异步萃取**：后台任务定期从 `dialogue_history` 中提取新的隐式偏好，更新 `extracted_preferences`。

#### 14.5.1 动态权限控制
- **基于角色的访问控制 (RBAC)**：
  - `dept_code` 决定了默认访问的知识库范围。
  - `job_title` 决定了操作权限（如是否可执行高风险操作）。

根据上一步骤输出的意图执行分发处理，RAG流程或Agent处理，对应承接动作请见：[意图体系.csv]




# 角色
你是一个专业的银行语言解析判定器。你的任务是分析用户的当前提问（Query）在没有上下文辅助时，是否能够作为一个独立完整的需求直接送入意图识别模块。

# 判定规则
如果当前提问表意含糊、缺少主宾、省略了关键业务实体名词，或者是个反问、口语化极强的长篇大论，必须返回 "True"（代表需要被提取改写）。
如果当前提问主题明确、主语齐全、名词完整，像一个可以直接在系统中搜索的标准问题，请直接返回 "False"（代表结构完整，不需要改写）。

# 要求
仅允许输出 "True" 或 "False"，除此之外绝对不要输出任何其他原因或标点。

# 示例
## 示例 1 
提问：“你好，我想开通个人网银怎么操作？”
输出：False
## 示例 2
提问：“可是如果我在APP里找不到呢”
输出：True 
## 示例 3
提问："那白金卡的呢"
输出：True


# 输入数据
[历史上下文]：{{history}}
[当前提问]：{{user_input}}

# 输出数据
{
"need_rewrite":True/False,
}